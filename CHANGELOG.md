# 更新日志

本文档按发布时间倒序记录 Claude Workspace 正式发布版本的用户可见变更。尚未打包发布的源码变更不在此记录。

## 0.5.1 - 2026-08-31

本版本修复了部分 Git 版本无法打开文件变更侧栏的问题，并提升了界面文字的清晰度。

### 优化

- 将功能入口名称从“修改文件”统一调整为“文件变更”，准确覆盖新增、删除、重命名和未跟踪等状态。
- 提升侧栏、顶部操作区、快速切换和企业微信设置中的文字字号、字重与对比度。
- 提升文件变更列表、代码对比和 Markdown 预览的阅读字号，并保持最小窗口尺寸下的布局完整。

### 修复

- 改用兼容旧版 Git 的 `git status --porcelain` 参数，修复部分 Git 版本因报错 `option 'porcelain' takes no value` 而无法查看文件变更的问题。

## 0.5.0 - 2026-08-27

本版本新增了工程文件变更查看能力。

### 新增

- 工程会话新增文件变更侧栏，可查看当前 Git 工作区的变更文件列表。
- 支持查看文件相对 `HEAD` 的差异，以及磁盘上的最新文件内容。
- 代码内容支持语法高亮；Markdown 支持 GFM 与 Mermaid 图表预览，并可切换查看源码。
- 文件变更列表每 4 秒自动刷新，同时支持手动刷新。

## 0.4.13 - 2026-08-20

本版本改进了企业微信远程回复的菜单选项匹配。

### 修复

- 菜单选项文字改为规范化后的完整、唯一匹配，避免相似或部分相同的文字被误选。
- 多个选项规范化后文本相同时，不再自动选择其中一个，而是提示改用通知中的选项编号。
- 数字回复优先按选项编号处理，并校验编号范围，避免无效编号进入终端。
- 统一支持全角与半角字符、英文大小写、连续空格以及选项末尾的 `(Esc)` 标记。
- 支持直接回复“输入其他回答（Type something.）”和“与 Claude 讨论这个问题（Chat about this）”等完整选项文字。
